name: Build and deploy

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    name: update
    uses: wei/curl@v1
    with:
      args: https://l3p3.de/svr/site-update.txt?site=Hoffsite
